<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pawa.ec.udomidrive_demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.User.UserLocationActivity"></activity>
        <activity android:name=".UI.Driver.DriverLocationActivity" />
        <activity android:name=".UI.User.Verification2Activity" />
        <activity android:name=".UI.Driver.LoginDriverActivity" />
        <activity android:name=".UI.Driver.CuentaActivity" />
        <activity android:name=".UI.User.DatosUserActivity" />
        <activity android:name=".UI.User.Verification1Activity" />
        <activity android:name=".UI.User.RutaEscogidaActivity" />
        <activity android:name=".MainActivity4" />
        <activity android:name=".UI.User.ResumenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key" />
    </application>

</manifest>